# Postgres (docker compose) — used by drizzle-kit scripts
POSTGRES_USER=chat
POSTGRES_PASSWORD=chat
POSTGRES_DB=chat
POSTGRES_PORT=5432
DATABASE_URL=postgres://chat:chat@localhost:5432/chat

# Local Worker DB: see hyperdrive.localConnectionString in wrangler.jsonc
# (defaults to the same docker postgres URL)
